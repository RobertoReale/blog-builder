## FEATURE 02 — Reading UX

The blog foundation is already built. Add the following reading
experience improvements to the existing codebase.
Do not change any existing functionality or design.
All features are progressive enhancements — if JS is disabled, nothing breaks.

CRITICAL INSTRUCTION: Before writing any code, use your web search tools to:
1. Check for the most current and widely used implementations of reading progress bars, table of contents (with scroll-spy), and clipboard share APIs in Astro.
2. Verify that a vanilla-JS approach (no third-party library) is still the norm for these reading UX features — if a widely adopted, well-maintained Astro library for any of them has emerged, add a `<!-- ALTERNATIVE: <name> — <reason> -->` comment in the relevant component file, but implement what is specified here.
If you encounter a bug you cannot resolve by reading the code, search online for known issues and solutions before giving up.

---

### 1. Reading progress bar

Where: article pages only (/article/[slug])
Position: fixed, top of viewport, above everything (z-index high)
Height: 3px
Color: var(--color-accent)
Width: 0% → 100% as user scrolls from top to bottom of article
Transition: none (real-time, no lag)

Implementation:
- Add a <div id="progress-bar"> in ArticleLayout.astro
- Small inline <script> that listens to scroll and updates width
- Calculate: scrollY / (documentHeight - viewportHeight) * 100

CSS:
```css
#progress-bar {
  position: fixed;
  top: 0;
  left: 0;
  height: 3px;
  width: 0%;
  background: var(--color-accent);
  z-index: 9999;
  pointer-events: none;
}
```

---

### 2. Back to top button

Where: all pages
Appears: only after user scrolls down 400px
Position: fixed, bottom-right (24px from edges)

Design:
- Small square button, 40×40px
- Background: var(--color-bg), border: 1px solid var(--color-border)
- Icon: upward arrow SVG (inline, 16px), color var(--color-muted)
- On hover: border color becomes var(--color-accent)
- Hidden by default (opacity 0, pointer-events none)
- Visible when scrollY > 400 (opacity 1, pointer-events auto)
- Smooth opacity transition: 200ms
- On click: window.scrollTo({ top: 0, behavior: 'smooth' })
- IMPORTANT: Do not use an is:inline script. Use a standard <script> and wrap the button initialization in document.addEventListener('astro:page-load', () => { ... }) so it survives ViewTransitions navigations. The window scroll listener can be attached at the top level.

Add the component (BackToTop.astro) to BaseLayout so it appears on every page.

---

### 3. Share button (copy link)

Where: article header, after the tags row
Design: small button, DM Sans, 14px, var(--color-muted)
Default text: "Copy link" with a small link icon (inline SVG)
On click: navigator.clipboard.writeText(window.location.href)
Feedback: button text changes to "Copied!" for 2 seconds, then reverts
No social widgets, no external services

Fallback if clipboard API unavailable:
- Select a hidden <input> with the URL and copy that way

---

### 4. Table of Contents

Where: article pages only
Position: above the article body (after header)
Render only if article has 3 or more headings (h2 or h3).
If fewer than 3 headings: render nothing.

IMPORTANT: The TOC must be built from standard markdown headings (h2, h3) present in the rendered HTML.

Step 1 — Create TOC.astro (src/components/ui/TOC.astro)
- Renders an empty container div with id="toc-container"
- A client-side script runs after DOM ready and:
  1. Queries all `h2, h3` elements within the article body
  2. If count < 3: hides the container (do not render)
  3. If count >= 3: builds and injects the TOC HTML

Client-side script:
```js
document.addEventListener('astro:page-load', () => {
  const articleBody = document.querySelector('article');
  if (!articleBody) return;
  const headings = [...articleBody.querySelectorAll('h2, h3')];
  const container = document.getElementById('toc-container');
  if (!container || headings.length < 3) {
    if (container) container.style.display = 'none';
    return;
  }
  
  // Ensure headings have IDs
  headings.forEach(h => {
    if (!h.id) {
      h.id = h.textContent.toLowerCase().trim().replace(/[^\w\s-]/g, '').replace(/[\s_]+/g, '-').replace(/-+/g, '-');
    }
  });

  // Build DOM programmatically — never interpolate heading text into innerHTML
  // (heading titles can contain characters like < > & that break HTML if injected raw)
  const label = document.createElement('p');
  label.className = 'toc-label';
  label.textContent = 'Contents';
  container.appendChild(label);

  const ul = document.createElement('ul');
  ul.className = 'toc-list';
  headings.forEach(h => {
    const li = document.createElement('li');
    li.className = `toc-${h.tagName.toLowerCase()}`;
    const a = document.createElement('a');
    a.href = `#${h.id}`;
    a.textContent = h.textContent ?? '';
    li.appendChild(a);
    ul.appendChild(li);
  });
  container.appendChild(ul);
});
```

UI design:
- Label "Contents": DM Sans, 11px, uppercase, letter-spacing 0.08em,
  var(--color-muted)
- List items: DM Sans, 15px, var(--color-muted), no list-style-type
- Hover: var(--color-text), underline
- Outer block: border-left 2px solid var(--color-border), padding 16px 20px
- No background color change
- Mobile: collapsed behind a "Contents ↓" toggle button (details/summary
  HTML element is the simplest approach — no extra JS)
  Desktop: always expanded

Active heading highlight (optional but recommended):
- Use IntersectionObserver to watch headings
- When one enters the viewport: add active class to its TOC link
- Active link: var(--color-accent), no other decoration

Step 2 — Add TOC to article layout
In src/pages/article/[slug].astro, render <TOC /> in the same position
as before: after the article header, before the article body.

---

### Checklist before finishing
- [ ] Progress bar visible and moves correctly on article pages
- [ ] Back to top button appears after 400px scroll on all pages
- [ ] Share button copies URL and shows "Copied!" feedback
- [ ] TOC appears on example article (which has headings)
- [ ] TOC collapses on mobile via details/summary
- [ ] TOC does not appear on pages/articles with < 3 headings
- [ ] `npm run build` succeeds
- [ ] None of these features break if JS is disabled
