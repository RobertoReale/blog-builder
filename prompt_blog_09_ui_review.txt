## FINAL STEP — UI, UX & Design Quality Review

All features are built and E2E-tested. This is the final quality gate.
Read CLAUDE.md first — it is the authoritative source for the design system.

Your goal: audit every page and component for visual consistency,
accessibility, and UX polish. Fix every issue you find.
Do not add new features — only fix what is already there.

SCALABILITY REQUIREMENT: The design must look and work correctly even if the
blog grows to hundreds of articles and dozens of tags. Verify that:
- Article listing pages paginate cleanly and never show an unmanageable wall of cards
- Tag filter pills do not break the layout when there are many tags (they must
  scroll or wrap without causing page-level overflow)
- Article cards handle long titles, many tags, and long descriptions without
  breaking alignment or overflowing their container
- Tag pages and series pages render correctly regardless of how many articles
  they contain
The blog must be fully usable on mobile at every scale — a reader on a phone
with hundreds of articles available must have the same quality experience as
a reader on desktop with only a handful.

CRITICAL INSTRUCTION: Before fixing anything, use your web search tools to
look up current Astro accessibility patterns, WCAG 2.1 AA requirements for
personal blogs, and any known dark-mode or CSS variable pitfalls in the current Astro version.
Also check whether any of the UI/UX patterns specified here (share button, TOC,
back-to-top, dark mode) have been superseded by a widely adopted, better
approach — if so, note findings as `<!-- ALTERNATIVE: <name> — <reason> -->`
comments in the relevant component files, but only fix what is specified.
If you encounter a bug you cannot resolve from the code alone, search online
before giving up.
Your research findings always take precedence over any package versions, URLs, API signatures, or code patterns shown in this prompt. Treat all implementation examples as reference patterns — update them to match current reality based on what you find.

---

### 1. Design System Consistency

Grep all .astro, .css, and .ts files for raw hex color values:

  Pattern: #[0-9a-fA-F]{3,6}\b

The ONLY file allowed to contain hex values is src/styles/global.css.
Every hex found elsewhere must be replaced with the appropriate CSS variable:

  --color-bg       → page background
  --color-surface  → card/callout/code-block background
  --color-text     → primary text
  --color-muted    → secondary text, dates, metadata, captions
  --color-accent   → links, active states, focus rings, progress bar
  --color-border   → dividers, card outlines, TOC left border

Fix every violation you find.

---

### 2. Typography & Layout

Open src/styles/global.css and verify:
- Base font size is 18px (body or html rule)
- Line height is 1.75
- Max content width on article prose is 680px, centered
- Heading font (from CLAUDE.md STACK → "Fonts:" line) applied to h1, h2, h3
- Body font applied to body and UI elements
- h1, h2, h3 sizes form a clear visual hierarchy (h1 largest)

Open src/layouts/ArticleLayout.astro (or wherever the article body is rendered):
- The article <main> or prose container must have a max-width of ~680px
- Content must be horizontally centered

Fix any typography or layout inconsistency found.

---

### 2b. Prose Content Readability

The `.prose` class wraps MDX article body content. Verify that
`src/styles/global.css` contains **explicit** rules for every item below.
"Explicit" means an actual CSS rule — not a comment, not a browser default,
not a Tailwind utility. If any rule is missing or under-specified, add it.

**Heading sizing — the single most common readability failure.**
Without explicit sizes, h2/h3 in prose collapse to near-body-text size.
Verify:
- `.prose h2` has `font-size` at least 1.35rem, `font-weight: 700`, and
  `margin-bottom` of at least 0.6rem so it reads as a section break.
- `.prose h3` has `font-size` at least 1.05rem, `font-weight: 700`, and
  `margin-top` of at least 1.5rem so sub-sections breathe.
- The size gap between h1 (article title) → h2 → h3 is clearly perceptible.
  Reject if h2 and body text differ by less than 4px.

If either rule is missing or sizes are too small, set:
```css
.prose h2 { font-size: 1.45rem; font-weight: 700; line-height: 1.3;
            margin-top: 0.5rem; margin-bottom: 0.75rem; letter-spacing: -0.01em; }
.prose h3 { font-size: 1.1rem;  font-weight: 700; line-height: 1.4;
            margin-top: 1.75rem; margin-bottom: 0.5rem; }
```

**Horizontal rule spacing.**
Articles use `---` between sections. Verify `.prose hr` has `margin: 2.5rem 0`
(or equivalent). Without it, section breaks visually disappear.

**Lists — indentation.**
Verify `.prose ul, .prose ol` has `padding-left: 1.5rem` and
`.prose li` has `margin-bottom: 0.375rem`.
Browser default padding-left is 40px (too wide) or 0px depending on reset;
specify it explicitly.

**Blockquote — border and padding.**
Verify `.prose blockquote` has:
- `border-left: 3px solid var(--color-accent)` (accent, not muted border)
- `padding: 0.375rem 0 0.375rem 1.25rem`
- `margin: 1.75rem 0`
- `.prose blockquote strong { color: var(--color-text); }` so bold labels
  inside blockquotes stay readable (they inherit muted color otherwise).

**Code blocks — padding.**
Verify `.prose pre` has `padding: 1.25rem 1.5rem` and `line-height: 1.7`.
A pre with no explicit padding has code text touching the border.

**Tables — the most frequently missing style block.**
Verify all of the following exist. If the table block is absent entirely,
add it in full:

```css
.prose table {
  width: 100%; border-collapse: collapse; font-size: 0.9rem;
  margin: 2rem 0; display: block;
  overflow-x: auto; -webkit-overflow-scrolling: touch;
}
.prose thead th {
  text-align: left; font-size: 11px; font-weight: 600;
  text-transform: uppercase; letter-spacing: 0.07em;
  color: var(--color-muted); padding: 8px 14px;
  border-bottom: 2px solid var(--color-border); white-space: nowrap;
}
.prose tbody td {
  padding: 10px 14px; border-bottom: 1px solid var(--color-border);
  vertical-align: top; line-height: 1.5;
}
.prose tbody tr:last-child td { border-bottom: none; }
.prose tbody tr:nth-child(even) td { background-color: var(--color-surface); }
```

Replace `--color-surface` with the correct surface/alternate background
variable if CLAUDE.md uses a different name (e.g. `--color-origin-bg`).

**Dark mode — surface color in tables and code blocks.**
In dark mode, `--color-surface` must be a dark shade (not white).
Verify the `[data-theme="dark"]` block in global.css defines `--color-surface`
as a dark background. If the table alternating rows or code blocks appear
white-on-dark, the dark mode surface variable is wrong — fix it.

After all checks: open the example article in a browser (or read the rendered
HTML) and confirm that:
- Section headings (h2) are clearly larger than body text
- Horizontal rules have generous whitespace above and below
- Any markdown table has visible header styling and alternating row tints
- Code blocks have interior padding (text does not touch the border)
- Blockquotes have an accent-color left border

---

### 3. Dark Mode Completeness

Verify the FOUC-prevention script is present in src/layouts/BaseLayout.astro
inside the <head>, before any stylesheets:

```html
<script is:inline>
  const theme = localStorage.getItem('theme') ||
    (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
  document.documentElement.setAttribute('data-theme', theme);
</script>
```

If missing, add it.

Check src/styles/global.css:
- A [data-theme="dark"] block exists with all six color variables redefined
- No color value appears in only one theme (light/dark must both define every
  interactive state — hover, active, focus)

Check every component for hardcoded colors that would not adapt to dark mode
(see Section 1 above). Dark mode fails silently — text can become invisible
(e.g., color: black on a dark background) and the only way to catch it is to
read the source.

---

### 4. Accessibility (WCAG 2.1 AA)

Audit every component file in src/components/:

**Images:**
- Every <img> tag must have a non-empty alt attribute
- Decorative images (purely visual, no information) should use alt=""
- The ImageWithCaption.astro component must pass alt through to <img>

**Icon-only buttons:**
- ThemeToggle.astro: aria-label="Toggle dark mode"
- BackToTop.astro: aria-label="Back to top" (or equivalent)
- ShareButton / share button in the article header: aria-label or visible text

**Links:**
- Navigation links must have descriptive text (not bare URLs or icons without labels)
- "Read more" links (if any) must include the article title for context:
  e.g., aria-label="Read more: Article Title"

**Focus styles:**
- Search src/styles/global.css for `outline: none` or `outline: 0`
- If found and not replaced with a custom focus ring, add one:
  ```css
  :focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
  }
  ```

**Semantic HTML:**
- BaseLayout.astro must use <header>, <main>, <footer> as landmarks
- Article pages must wrap the article body in <article>
- Navigation must be in a <nav> element

**Heading hierarchy (check each page template):**
- Each page renders exactly one <h1>
- Heading levels are never skipped (h1 → h2 → h3, never h1 → h3)
- The article title in ArticleLayout.astro is an <h1>
- TOC links reference h2/h3 headings — verify no h4/h5/h6 headings are missed
  (this is acceptable; TOC only needs h2/h3)

**Search input:**
- src/pages/search.astro: the search input has an aria-label or an associated <label>

Fix every accessibility issue found.

---

### 5. Responsive Design & Mobile UX

Test at 375px width (iPhone SE / smallest common phone).
Fix every issue you find — the blog must be fully usable on mobile.

**5a. Viewport meta tag (critical foundation)**

Open src/layouts/BaseLayout.astro and verify the <head> contains:
```html
<meta name="viewport" content="width=device-width, initial-scale=1">
```
Without this, the entire blog renders at desktop scale on mobile and is unusable.
If missing, add it as the first meta tag inside <head>.

**5b. Touch targets**

All interactive elements must have a minimum clickable area of 44×44px
(WCAG 2.5.5 / iOS Human Interface Guidelines / Android Material guidelines).

Audit every component:
- ThemeToggle button in the nav
- BackToTop button (raise to 44×44px if currently 40×40px)
- Tag pills (ArticleCard.astro, TagPill.astro, TagFilter.astro) — add vertical
  padding so the tap target is at least 44px tall even if the visual pill is smaller
- Share button in the article header
- TOC links (padding: 8px 0 each link)
- SeriesNav prev/next links
- Nav links in BaseLayout.astro

Add padding to reach 44×44px. Do NOT change the visual appearance — add padding
that expands the hit area without changing the rendered size (e.g., use inline
padding on the element and negative margin or a wrapper to keep layout intact).

**5c. Navigation on mobile**

Open src/layouts/BaseLayout.astro.
At 375px width, the nav bar must:
- Not overflow horizontally or push content off screen
- Not truncate the blog title/logo
- Keep all links reachable without horizontal scroll
- Allow links to wrap to a second line if needed (flex-wrap: wrap)

Verify the nav container uses something like:
```css
display: flex;
flex-wrap: wrap;
align-items: center;
gap: 16px;
```

If nav items are crammed: reduce font size at small breakpoints or add a mobile
layout where the title sits on its own line above the links.

**5d. Article body on mobile**

- Horizontal padding: the article content area must have at least 16px of
  horizontal padding at mobile widths so text does not touch the screen edge.
  The main container should use padding: 0 1rem (or Tailwind px-4) at mobile.
- Max-width: `width: 100%; max-width: 680px; margin: 0 auto` — must not cause
  horizontal scroll at 375px.
- Code blocks: must have `overflow-x: auto` so long lines scroll horizontally
  within the block instead of pushing the page layout wider. Check global.css
  for `pre, code { overflow-x: auto; }`. Add if missing.
- Tables: if any article uses markdown tables, wrap them in a div with
  `overflow-x: auto` so they scroll horizontally on narrow screens.

**5e. Tag filter pills on mobile**

The row of tag pills (TagFilter.astro on homepage and articles page) must not
cause the page itself to scroll horizontally.

If pills overflow their container:
- Set the pills container to `overflow-x: auto; white-space: nowrap;`
- Hide the scrollbar visually while keeping scroll behavior:
  ```css
  .category-scroll { /* or whatever the container class is */
    scrollbar-width: none; /* Firefox */
  }
  .category-scroll::-webkit-scrollbar {
    display: none; /* Chrome/Safari */
  }
  ```
- Use `-webkit-overflow-scrolling: touch` for smooth scrolling on iOS.

**5f. TOC on mobile**

- TOC.astro must collapse on mobile via a `<details>/<summary>` structure.
  The `<summary>` should read "Contents" or "Contents ↓".
- On mobile, the default state is closed (no `open` attribute on `<details>`).
- On desktop (≥ 768px), the TOC must always be expanded.
  The reliable cross-browser way is JS + CSS. In the existing TOC client-side
  script (inside the `astro:page-load` listener, after building the TOC content),
  add:
  ```js
  const detailsEl = container.querySelector('details');
  if (detailsEl && window.matchMedia('(min-width: 768px)').matches) {
    detailsEl.open = true;
  }
  ```
  Then in global.css, hide the summary toggle on desktop:
  ```css
  @media (min-width: 768px) {
    #toc-container summary { display: none; }
  }
  ```
  Do NOT use `display: contents` on `<details>` — it is unreliable across
  browsers for controlling the native open/closed state.
- TOC links must have adequate tap targets (at least 44px height via padding).

**5g. BackToTop button on mobile**

- Must be at least 44×44px (touch target — increase from 40×40px if needed).
- Position: bottom-right, with at least 20px from the bottom edge and 20px from
  the right edge to keep it within safe areas on notched phones.
- Must not overlap or obscure the article body, footer, or related articles section.

**5h. Share button — Web Share API on mobile**

On mobile, the standard UX is the native share sheet (not clipboard copy).
Read the existing share button implementation (in the article page or a dedicated
component) and update its click handler to prefer Web Share API when available:

```js
// Wrap the existing click handler — find the share button by reading
// the actual component file (ArticleLayout.astro or [slug].astro).
// Replace the click handler body with:
if (navigator.share) {
  try {
    await navigator.share({ title: document.title, url: window.location.href });
    return;
  } catch (err) {
    if (err.name !== 'AbortError') console.error(err);
    return; // user cancelled — do not fall through to clipboard
  }
}
// If navigator.share is not available, run the existing clipboard copy logic.
```

Make the click handler async if it is not already.
This gives mobile users the native share sheet (WhatsApp, Messages, copy link,
etc.) while desktop users keep the existing clipboard copy flow.

**5i. Images on mobile**

- All `<img>` elements inside article content must be responsive:
  `max-width: 100%; height: auto;`
- Check global.css for `img { max-width: 100%; height: auto; }` and add if absent.
- ImageWithCaption.astro: the image must not overflow its container at 375px.
  If size="half" is used on mobile, consider resetting to 100% below a breakpoint.

**5j. Search input — prevent iOS zoom**

iOS Safari auto-zooms the page when the user taps an input with font-size < 16px.
Open src/pages/search.astro and src/styles/global.css.
Ensure the search input has `font-size: 1rem` (16px) or larger.
If it uses a smaller size, add a mobile override:
```css
@media (max-width: 768px) {
  input[type="search"], input[type="text"] {
    font-size: 1rem;
  }
}
```

**5k. Horizontal overflow — no page-level scroll**

Verify the page does not scroll horizontally at 375px. The safest fix is:
```css
html {
  overflow-x: clip; /* 'clip' does NOT break position: sticky, unlike 'hidden' */
}
```
Add `overflow-x: clip` to the `html` rule in global.css if not already present.
Do NOT use `overflow-x: hidden` on `body` — it breaks `position: sticky` elements
like the reading progress bar.

**5l. Reading progress bar on mobile**

- The 3px bar at the top of article pages must sit above the content and nav,
  not behind them. Verify z-index is high (9999 or similar).
- On iOS Safari with safe-area insets, the bar should appear below the notch.
  This is generally handled automatically by the browser, but verify the bar
  uses `top: 0` and not a negative top value.

**5m. SeriesNav and RelatedArticles on mobile**

- SeriesNav (prev/next links): at 375px, both prev and next links may sit in the
  same row and overflow. They should either wrap to separate lines or stack as a
  column. Ensure the container uses `flex-wrap: wrap` or `flex-direction: column`
  at narrow widths so neither link is cut off or hidden.
- RelatedArticles: the component uses a CSS grid with
  `grid-template-columns: repeat(auto-fill, minmax(180px, 1fr))` (specified in
  Step 6). At 375px viewport with 16px side padding, available width ≈ 343px,
  which fits exactly one 343px column — so the grid collapses to 1 column
  automatically. Verify this is the case and that no card text overflows or is
  clipped at 375px width.

Fix all responsive and mobile issues found.

---

### 6. Interactive States & Hover Feedback

**Active navigation state:**

In BaseLayout.astro, the current page's nav link must be visually distinct.
Use `Astro.url.pathname` to determine the active link and apply `var(--color-text)`
instead of `var(--color-muted)`. Add these lines in the frontmatter:

```ts
const pathname = Astro.url.pathname;
const navColor = (href: string) =>
  (href === '/' ? pathname === '/' : pathname === href || pathname.startsWith(href + '/'))
    ? 'var(--color-text)'
    : 'var(--color-muted)';
```

Then replace the hardcoded `color: var(--color-muted)` on each nav `<a>` with
`style={`color: ${navColor('/articles')}`}` (and so on for each link).
The external Portfolio link always stays muted (cannot be active).

Every clickable element must also have a visible hover state. Audit:

- **Navigation links**: color change on hover
- **Article card titles/links**: underline or color change on hover
- **Tag pills** (ArticleCard.astro, TagPill.astro): background or border change
- **ThemeToggle**: opacity or color change on hover
- **BackToTop button**: border color changes to var(--color-accent) on hover
- **Share button**: border or text color changes on hover
- **TOC links**: color change to var(--color-text) + underline on hover;
  active heading link uses var(--color-accent)

If any hover state is missing, add it using CSS variables only (never hex).

---

### 7. Page-by-Page Audit

Read the source of each page template and verify the following:

**Homepage (src/pages/index.astro):**
- ArticleCard components are rendered for each published article
- Tag filter pills exist (TagFilter or similar component)
- If no published articles exist, a graceful empty state is shown
- ArticleCards for non-English articles show a language badge (e.g. "IT")

**Articles page (src/pages/articles/[...page].astro):**
- Paginated article list (10 per page), without the hero/intro text and without TagFilter
- Pagination navigation is present (← Newer / Older →) and links only appear for pages that exist
- ArticleCards for non-English articles show a language badge (e.g. "IT")

**Article page (src/pages/article/[slug].astro):**
Verify the render order matches the CLAUDE.md specification exactly:
  1. Header (title, date, reading time, tags, share button)
  2. SeriesNav (if article is in a series)
  3. TOC (if article has ≥ 3 headings)
  4. Article body (MDX)
  5. Sources
  6. --- separator ---
  7. Related articles (if any)
  8. Footer

If the order is wrong, fix it.

Also verify that Sources is rendered **explicitly** from frontmatter data, not only
available as an MDX component. After the article body (`<Content />`), there must be
a call like:
```astro
<Sources sources={entry.data.sources ?? []} />
```
This is separate from passing `Sources` in the MDX components map. The MDX components
map makes it usable inside article body text; the explicit call renders the frontmatter
`sources` array automatically for every article. Both are required.

**Search page (src/pages/search.astro):**
- Search input is present and has an accessible label
- Keyboard shortcut "/" triggers focus on the search input when pressed
  anywhere on the page (not while already in an input field)

**About page (src/pages/about.astro) and Now page (src/pages/now.astro):**
- Both pages exist and render without errors
- Both have meaningful placeholder content (not completely empty)
- Any TODO markers are clear and descriptive

**404 page (src/pages/404.astro):**
- Exists
- Has a friendly message and a link back to the homepage

**RSS feed (src/pages/rss.xml.ts):**
- File exists
- Uses the SITE constants from src/config.ts (not hardcoded values)

**Tag pages (src/pages/tag/[tag].astro):**
- Renders a filtered article list for the given tag
- Tag name is shown in the page heading

**Series pages (src/pages/series/[series].astro):**
- Renders articles in the series, ordered by part number
- Series name is shown in the heading

---

### 8. Example Article Quality

Open the example article in src/content/articles/ (the one created in Step 0).

Verify:
- status: published (so it appears in the listing and triggers TOC rendering)
- date is a past date (not far in the future)
- The article body has at least 3 h2 or h3 headings (so the TOC renders)
- The body has meaningful text (not placeholder Lorem ipsum)
- There is no `coverImage:` line with an empty value (would cause Zod error)
- The title is descriptive (not "Example" or "Test")

If any of the above are wrong, fix them.

---

### 9. Configuration Completeness

The user entered their blog title, URL, author name, description, color
palette, and fonts at the beginning (via setup.ps1 / setup.sh). Those values
were written into CLAUDE.md and Step 0 used them to create the blog. This
check verifies nothing was left as a generic placeholder.

**Step A — Read the authoritative values from CLAUDE.md.**

Open CLAUDE.md and note the values under "Site values":
- title
- description
- url
- author

Also note the fonts listed under "Fonts:" in the STACK section,
and the hex values in the CSS color block.

**Step B — Verify src/config.ts matches.**

Open src/config.ts and check that the four SITE fields match CLAUDE.md exactly.

If any field still contains a generic placeholder, replace it with the value
from CLAUDE.md:

Placeholder patterns that must NOT appear in src/config.ts:
- title: "My Blog" / "Your Blog" / "Blog" / "Astro Blog"
- url: "https://example.com" / "http://localhost" / "https://yourdomain.com"
- author: "Your Name" / "Author" / "John Doe"
- description: "" / "A personal blog" / "My personal blog"

**Step C — Verify fonts in src/styles/global.css.**

Fonts are self-hosted via @fontsource — there should be NO Google Fonts <link> tag
in BaseLayout.astro. If one exists, remove it.

Read the font names from CLAUDE.md's STACK section (the "Fonts:" line).
Open src/styles/global.css and verify that @import lines for the correct
@fontsource packages appear at the top (e.g. `@import '@fontsource/lora/400.css'`
for Lora). The exact package name is the font name lowercased with hyphens
(e.g. "DM Sans" → `@fontsource/dm-sans`).

Also verify that the corresponding @fontsource packages are listed in
package.json dependencies. If any package is missing, install it.

Then add the @import lines to the top of global.css if absent.

**Step D — Verify CSS color variables in global.css.**

Open src/styles/global.css. The hex values in the :root block must match
the colors from CLAUDE.md exactly.

If you find the unmodified default palette:
  --color-bg: #FFFFFF; --color-text: #111827; --color-accent: #2563EB; etc.

but CLAUDE.md specifies different values, replace the global.css values with
the correct ones from CLAUDE.md.

**Step E — Grep for hardcoded author/title strings in page files.**

Run: grep -r "Your Name\|Your Blog\|example\.com\|yourdomain\|Author Name\|John Doe" src/ astro.config.mjs

Any match outside of comments must be replaced with the appropriate SITE
constant imported from src/config.ts:
  import { SITE } from '../config';
  // then use SITE.title, SITE.author, SITE.url, SITE.description

Fix every placeholder found.

---

### 10. Build & Final Verification

After all fixes:

1. Run: npm run build
   Must succeed. Fix any errors until it passes.

2. Run: npm run test:unit
   Must pass. Fix any broken unit tests.

3. If npm run test:e2e exists in package.json, run it as well.
   Fix any Playwright failures that were introduced by your changes.

---

### Checklist before finishing

- [ ] No hardcoded hex colors outside src/styles/global.css
- [ ] All CSS variables used correctly throughout every component
- [ ] Base font 18px, line-height 1.75, max-width 680px in global.css
- [ ] Heading font and body font applied correctly
- [ ] Dark mode block in global.css is complete
- [ ] .prose h2 has font-size ≥ 1.35rem and font-weight: 700 (explicit, not browser default)
- [ ] .prose h3 has font-size ≥ 1.05rem and font-weight: 700 (explicit, not browser default)
- [ ] .prose hr has margin: 2.5rem 0 (section breaks breathe)
- [ ] .prose ul, .prose ol have explicit padding-left and .prose li has margin-bottom
- [ ] .prose blockquote has accent-color left border, padding, and strong override
- [ ] .prose pre has explicit padding (1.25rem 1.5rem) and line-height: 1.7
- [ ] .prose table block is present with th/td padding, border-bottom, and alternating rows
- [ ] Tables use display: block + overflow-x: auto (mobile-safe)
- [ ] --color-surface (or equivalent) is dark in [data-theme="dark"] (tables/code readable)
- [ ] FOUC-prevention script present in BaseLayout.astro <head>
- [ ] All <img> tags have alt attributes
- [ ] ThemeToggle, BackToTop, and share button all have aria-label
- [ ] Focus styles visible (outline not removed without replacement)
- [ ] Semantic landmarks: <header>, <main>, <footer>, <nav>, <article>
- [ ] One <h1> per page, no skipped heading levels
- [ ] Search input has accessible label
- [ ] Viewport meta tag (width=device-width, initial-scale=1) in BaseLayout.astro
- [ ] All interactive elements have ≥ 44×44px touch targets (nav, toggle, pills, share, TOC links, back-to-top)
- [ ] Nav links wrap cleanly at 375px without overflow or truncation
- [ ] Article body has ≥ 16px horizontal padding on mobile
- [ ] Code blocks have overflow-x: auto (no horizontal page scroll from long lines)
- [ ] Tag filter pills row scrolls horizontally on mobile, does not cause page overflow
- [ ] TOC collapses on mobile via <details>/<summary>; expanded by default on desktop
- [ ] BackToTop is at least 44×44px, positioned ≥ 20px from screen edges
- [ ] Share button uses Web Share API on mobile, clipboard fallback on desktop
- [ ] Images are responsive: max-width: 100%; height: auto in global.css
- [ ] Search input font-size ≥ 16px (prevents iOS auto-zoom)
- [ ] html { overflow-x: clip } prevents horizontal page scroll on mobile
- [ ] SeriesNav prev/next links wrap or stack at 375px (no overflow)
- [ ] RelatedArticles grid collapses to 1 column at 375px
- [ ] All interactive elements have visible hover states
- [ ] Active nav link uses var(--color-text); inactive links use var(--color-muted)
- [ ] Article render order matches the CLAUDE.md spec
- [ ] `<Sources sources={entry.data.sources ?? []} />` is called explicitly after the article body
- [ ] 404 page exists with a homepage link
- [ ] Example article has ≥ 3 headings, past date, status: published
- [ ] src/config.ts has the user's real title, URL, author, description
- [ ] @fontsource @import lines in global.css load the correct fonts (no Google Fonts <link> tag)
- [ ] CSS color variables in global.css match the user's chosen palette
- [ ] No hardcoded "Your Name" / "example.com" / "Your Blog" strings in src/
- [ ] /articles page is paginated (10 per page) with working ← Newer / Older → navigation
- [ ] ArticleCards show a language badge (e.g. "IT") for non-English articles
- [ ] npm run build succeeds
- [ ] npm run test:unit passes
